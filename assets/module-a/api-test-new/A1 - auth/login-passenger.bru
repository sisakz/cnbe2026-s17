meta {
  name: login-passenger
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/passenger/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  { "email": "passenger1@email.com", "password": "passenger123" }
}

script:post-response {
  if (res.status === 200) {
    bru.setEnvVar("passengerToken", res.body.data.token);
  }
}

tests {
  test("A1.6: Staff and passenger tokens are separate systems — each is rejected on the other's endpoints (401 both directions; documented exception: GET /terminals) (passenger pool issues its own token)", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data.token).to.be.a("string");
  });
}
