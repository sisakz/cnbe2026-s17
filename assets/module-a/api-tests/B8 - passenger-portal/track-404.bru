meta {
  name: track-404
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/claims/track/CL-NOTFOUND
  body: none
  auth: none
}

tests {
  test("B8: unknown tracking code returns 404", function() {
    expect(res.status).to.equal(404);
    expect(res.body.message).to.equal("Resource not found");
  });
}
