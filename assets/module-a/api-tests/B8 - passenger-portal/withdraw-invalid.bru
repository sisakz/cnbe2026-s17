meta {
  name: withdraw-invalid
  type: http
  seq: 14
}

post {
  url: {{baseUrl}}/passenger/claims/4/withdraw
  body: none
  auth: bearer
}

auth:bearer {
  token: {{passengerToken}}
}

tests {
  test("B8: a resolved claim cannot be withdrawn", function() {
    expect(res.status).to.equal(422);
  });
}
