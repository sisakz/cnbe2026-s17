meta {
  name: get-404
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/claims/9999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("B5: unknown claim returns 404", function() {
    expect(res.status).to.equal(404);
    expect(res.body.message).to.equal("Resource not found");
  });
}
