meta {
  name: status-not-allowed
  type: http
  seq: 7
}

patch {
  url: {{baseUrl}}/claims/19/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  { "status": "resolved" }
}

tests {
  test("A5: statuses owned by dedicated endpoints are rejected here", function() {
    expect(res.status).to.equal(422);
  });
}
