meta {
  name: status-validation-422
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/items
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {}
}

script:post-response {
  bru.setVar("statusValidationOk", res.status === 422 ? "ok" : "validation returned " + res.status);
}

