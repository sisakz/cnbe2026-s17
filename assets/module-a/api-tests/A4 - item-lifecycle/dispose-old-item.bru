meta {
  name: dispose-old-item
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/items/1/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  { "status": "disposed" }
}

tests {
  test("A4: item past the 60-day retention period can be disposed", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data.status).to.equal("disposed");
  });
}
