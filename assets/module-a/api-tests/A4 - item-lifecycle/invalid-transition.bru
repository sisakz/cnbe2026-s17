meta {
  name: invalid-transition
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/items/73/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  { "status": "in-storage" }
}

tests {
  test("A4: invalid transition (returned to in-storage) returns 422", function() {
    expect(res.status).to.equal(422);
  });
}
