meta {
  name: get-404
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/items/9999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("B3: unknown item returns 404", function() {
    expect(res.status).to.equal(404);
    expect(res.body.message).to.equal("Resource not found");
  });
}
