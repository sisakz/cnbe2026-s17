meta {
  name: suggestions-closed-claim
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/claims/36/matches
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("B6: suggestions are refused for a claim that is not open", function() {
    expect(res.status).to.equal(422);
  });
}
