meta {
  name: resolve-double
  type: http
  seq: 10
}

post {
  url: {{baseUrl}}/claims/2/resolve
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("A6: a resolved claim cannot be resolved again", function() {
    expect(res.status).to.equal(422);
  });
}
