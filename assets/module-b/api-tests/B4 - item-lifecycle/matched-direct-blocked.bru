meta {
  name: matched-direct-blocked
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/items/7/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  { "status": "matched" }
}

tests {
  test("B4: matched cannot be set directly on an item", function() {
    expect(res.status).to.equal(422);
  });
}
